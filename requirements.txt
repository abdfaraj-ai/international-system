# ══════════════════════════════════════════════════════════════
#  التبعيات الأساسية — نظام انترناشونال الموحد
#  تثبيت: pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════

# ── إطار العمل ──────────────────────────────────────────────
Django==4.2
asgiref==3.11.1
sqlparse==0.5.5
pytz==2024.1
requests==2.32.3

# ── قاعدة البيانات ───────────────────────────────────────────
# SQLite مدمج مع Python (للتطوير)
# للإنتاج: قم بتثبيت psycopg2-binary
psycopg2-binary==2.9.10

# ── خادم الإنتاج ─────────────────────────────────────────────
gunicorn==22.0.0
whitenoise==6.8.2
daphne==4.2.1

# ── WebSocket (Django Channels) ───────────────────────────────
channels==4.2.0

# ── المصادقة الثنائية 2FA (Google Authenticator) ─────────────
pyotp==2.9.0
qrcode[pil]==8.0

# ── CORS + Security ──────────────────────────────────────────
django-cors-headers==4.9.0

# ── معالجة الصور ──────────────────────────────────────────────
pillow==12.1.1

# ── إنشاء الوثائق ──────────────────────────────────────────────
python-docx==1.2.0
openpyxl==3.1.5
reportlab==4.4.10

# ── متغيرات البيئة (.env) ──────────────────────────────────────
python-dotenv==1.0.1

# ── OCR (اختياري — لتطبيق Flutter) ───────────────────────────
# easyocr
# pytesseract
# paddleocr

# ── الذكاء الاصطناعي (اختياري — لتطبيق Flutter) ──────────────
# openai-whisper
# torch
# torchvision
